{
  "name": "contribution-cityscape",
  "version": "1.0.0",
  "description": "Fetch and visualize GitHub contributions as a fair, adjustable 3D metric (PRs, reviews, commits — no issues).",
  "type": "module",
  "main": "fetch-data.js",
  "scripts": {
    "fetch": "node fetch-data.js",
    "serve": "npx http-server . -p 8080",
    "start": "npm run serve"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "engines": { "node": ">=18" },
  "license": "MIT"
}
