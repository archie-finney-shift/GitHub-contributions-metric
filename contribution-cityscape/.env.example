# GitHub personal access token.
# Classic token scopes needed: read:user (public data). Add "repo" if you need
# to include contributions to private repositories the token owner can see.
# Create one at https://github.com/settings/tokens
GITHUB_TOKEN=
